rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isDataOwner() {
      return isSignedIn() && request.auth.uid == resource.data.userId;
    }

    function isIncomingOwner() {
      return isSignedIn() && request.auth.uid == request.resource.data.userId;
    }

    function isAdmin() {
      return isSignedIn() && (request.auth.token.email == 'igor.cidade@hotmail.com' || request.auth.token.email == 'igrcidade@gmail.com');
    }

    // Default deny
    match /{document=**} {
      allow read, write: if false;
    }

    match /leads/{id} {
      allow create: if true;
    }

    match /users/{userId} {
      allow read: if isOwner(userId) || isAdmin();
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) || isAdmin();
      allow delete: if isAdmin();
    }

    match /profiles/{userId} {
      allow read: if isOwner(userId);
      allow write: if isOwner(userId);
    }

    match /services/{id} {
      allow read: if isDataOwner();
      allow create: if isIncomingOwner();
      allow update, delete: if isDataOwner() && isIncomingOwner();
    }

    match /products/{id} {
      allow read: if isDataOwner();
      allow create: if isIncomingOwner();
      allow update, delete: if isDataOwner() && isIncomingOwner();
    }

    match /clients/{id} {
      allow read: if isDataOwner();
      allow create: if isIncomingOwner();
      allow update, delete: if isDataOwner() && isIncomingOwner();
    }

    match /barbers/{id} {
      allow read: if isDataOwner();
      allow create: if isIncomingOwner();
      allow update, delete: if isDataOwner();
    }

    match /sales/{id} {
      allow read: if isDataOwner();
      allow create: if isIncomingOwner();
      allow update, delete: if isDataOwner() && isIncomingOwner();
    }

    match /expenses/{id} {
      allow read: if isDataOwner();
      allow create: if isIncomingOwner();
      allow update, delete: if isDataOwner() && isIncomingOwner();
    }
  }
}
